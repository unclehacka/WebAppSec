name: security-gates
on:
  pull_request:
  push:
    branches: [ main ]
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:
concurrency: security-gates-${{ github.ref }}

jobs:
  sast:
    name: SAST gates
    permissions:
      contents: read
      security-events: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }

      - name: Enforce lockfile
        if: hashFiles('package.json') != '' && hashFiles('package-lock.json') == ''
        run: |
          echo "::error::package-lock.json is missing. Run: npm install --package-lock-only"
          exit 1

      - name: npm ci (locked)
        if: hashFiles('package-lock.json') != ''
        run: npm ci --ignore-scripts

      - name: Semgrep (OWASP + custom) > SARIF
        run: |
          python3 -m pip install --upgrade pip
          pip3 install semgrep
          semgrep scan \
            --config p/owasp-top-ten \
            --config ./semgrep.yml \
            --severity ERROR --error --metrics=off \
            --sarif -o semgrep.sarif

      - name: Upload Semgrep SARIF
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: semgrep.sarif

      - name: Gitleaks (PR)
        if: github.event_name == 'pull_request'
        uses: gitleaks/gitleaks-action@v2
        with:
          args: detect --redact -v --exit-code=2 --report-format sarif --report-path gitleaks.sarif
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Gitleaks SARIF (PR)
        if: always() && github.event_name == 'pull_request' && hashFiles('gitleaks.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: gitleaks.sarif

      - name: Gitleaks (repo scan)
        if: github.event_name != 'pull_request'
        run: |
          ver=8.24.3
          curl -sSL https://github.com/zricethezav/gitleaks/releases/download/v${ver}/gitleaks_${ver}_linux_x64.tar.gz -o /tmp/gitleaks.tgz
          tar -xzf /tmp/gitleaks.tgz -C /tmp gitleaks
          install -m 0755 /tmp/gitleaks /usr/local/bin/gitleaks
          gitleaks detect --no-git --redact --exit-code 2 --report-format sarif --report-path gitleaks.sarif

      - name: Upload Gitleaks SARIF (repo)
        if: always() && github.event_name != 'pull_request' && hashFiles('gitleaks.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: gitleaks.sarif

      - name: Trivy filesystem
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          ignore-unfixed: true
          severity: HIGH,CRITICAL
          skip-dirs: node_modules
          exit-code: '1'

      - name: Syft SBOM
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin
          syft . -o json > sbom.json

      - name: Grype on SBOM
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin
          grype sbom:sbom.json --fail-on high
      - name: tfsec (Terraform)
        if: hashFiles('**/*.tf') != ''
        uses: aquasecurity/tfsec-action@v1.0.0
        with:
          soft_fail: false

      - name: Checkov (Terraform/K8s/CFN)
        if: hashFiles('**/*.tf') != '' || hashFiles('**/*.yml') != '' || hashFiles('**/*.yaml') != ''
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: .
          framework: terraform,kubernetes,cloudformation
          quiet: true
          soft_fail: false
          output_format: sarif
          output_file_path: checkov.sarif

      - name: Upload Checkov SARIF
        if: always() && hashFiles('checkov.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: checkov.sarif
  dast:
    name: DAST ZAP
    permissions:
      contents: read
      security-events: write
    if: ${{ always() && (github.event_name != 'push' || github.ref == 'refs/heads/main') }}
    needs: sast
    runs-on: ubuntu-latest
    timeout-minutes: 25
    services:
      juice:
        image: bkimminich/juice-shop@sha256:latest
        ports: [ "3000:3000" ]

    steps:
      - uses: actions/checkout@v4

      - name: Wait for target
        run: |
          for i in {1..60}; do curl -fsS http://localhost:3000 >/dev/null && exit 0; sleep 2; done
          echo "Target not healthy" >&2; exit 1

      - name: ZAP Baseline
        uses: zaproxy/action-baseline@v0.15.0
        with:
          target: "http://localhost:3000"
          rules_file_name: ".zap/rules.tsv"
          cmd_options: "-m 3 -d -I"
          fail_action: true
          allow_issue_writing: false
          artifact_name: "zap_baseline"

      - name: Trivy image (Juice Shop)
        if: always()
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'bkimminich/juice-shop@sha256:latest'
          vuln-type: 'os,library'
          severity: 'HIGH,CRITICAL'
          ignore-unfixed: true
          exit-code: '1'
          format: 'sarif'
          output: 'trivy-image.sarif'

      - name: Upload Trivy SARIF (image)
        if: always() && hashFiles('trivy-image.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: trivy-image.sarif

      - name: Upload ZAP reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap_baseline_reports
          path: |
            report_html.html
            report_md.md
            report_json.json
          retention-days: 14