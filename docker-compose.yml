version: '3.8'
services:
  juice:
    image: bkimminich/juice-shop
    ports: ["3000:3000"]
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:3000/"]
      interval: 5s
      timeout: 2s
      retries: 30